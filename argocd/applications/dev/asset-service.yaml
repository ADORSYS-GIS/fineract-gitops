apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fineract-dev-asset-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "6"  # Backend service - deploys WITH Fineract backend (wave 6)
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: fineract-dev

  source:
    repoURL: "https://github.com/ADORSYS-GIS/fineract-gitops.git"
    targetRevision: deploy-key
    path: apps/asset-service/base

  destination:
    server: https://kubernetes.default.svc
    namespace: fineract-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  info:
    - name: Component
      value: Asset Service - Digital asset management backend
    - name: Environment
      value: Development
