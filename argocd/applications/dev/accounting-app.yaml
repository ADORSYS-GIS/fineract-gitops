apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fineract-dev-accounting-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7"  # Frontend application - deploys AFTER Fineract backend (wave 6)
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: fineract-dev

  source:
    repoURL: "https://github.com/guymoyo/fineract-gitops.git"
    targetRevision: eks
    path: apps/accounting-app/base

  destination:
    server: https://kubernetes.default.svc
    namespace: fineract-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  info:
    - name: Component
      value: Accounting App - Financial accounting and ledger management
    - name: Environment
      value: Development
