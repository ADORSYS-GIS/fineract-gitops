# ==============================================================================
# ArgoCD Application: User Sync Service for GCP Dev Environment
# ==============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fineract-dev-gcp-user-sync
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default

  source:
    repoURL: "https://github.com/ADORSYS-GIS/fineract-gitops.git"
    targetRevision: feature/gcp-implementation
    path: apps/user-sync-service/overlays/dev-gcp

  destination:
    server: https://kubernetes.default.svc
    namespace: fineract-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  info:
    - name: Component
      value: User Sync Service
    - name: Environment
      value: GCP Development
    - name: Purpose
      value: "Keycloak to Fineract user synchronization"
