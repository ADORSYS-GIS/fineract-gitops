apiVersion: fineract.apache.org/v1
kind: Configuration
metadata:
  name: default-password
  labels:
    config-type: Security
spec:
  name: DEFAULT_PASSWORD
  enabled: true
  value: password
  description: ''
