apiVersion: fineract.apache.org/v1
kind: SMSEmailConfig
metadata:
  name: system-retry-delay-minutes
  labels:
    config-type: notification-settings
    provider: system
spec:
  configType: Notification Settings
  provider: System
  configKey: retry_delay_minutes
  configValue: '5'
  isActive: true
  description: Delay between retry attempts (minutes)
