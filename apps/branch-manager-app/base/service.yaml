apiVersion: v1
kind: Service
metadata:
  name: branch-manager-app
  labels:
    app: branch-manager-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: branch-manager-app