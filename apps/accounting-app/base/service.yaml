apiVersion: v1
kind: Service
metadata:
  name: accounting-app
  labels:
    app: accounting-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: accounting-app
