apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: logging
  labels:
    app.kubernetes.io/name: loki
  annotations:
    description: "Loki log aggregation service"
spec:
  type: ClusterIP
  ports:
  - name: http-metrics
    port: 3100
    targetPort: 3100
    protocol: TCP
  selector:
    app.kubernetes.io/name: loki
