apiVersion: v1
kind: Service
metadata:
  name: portal-app
  labels:
    app: portal-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: portal-app
