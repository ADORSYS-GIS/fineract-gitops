apiVersion: v1
kind: Service
metadata:
  name: cashier-app
  labels:
    app: cashier-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: cashier-app