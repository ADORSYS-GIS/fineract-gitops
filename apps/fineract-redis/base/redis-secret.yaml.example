# PLACEHOLDER - DO NOT COMMIT REAL SECRETS
#
# This file shows the structure of the Redis password secret needed for Fineract Redis.
#
# To create the actual secret:
#
# 1. Generate a strong password:
#    export REDIS_PASSWORD=$(openssl rand -base64 32)
#
# 2. Create a Kubernetes secret:
#    kubectl create secret generic fineract-redis-secret \
#      --from-literal=redis-password="$REDIS_PASSWORD" \
#      --namespace=fineract-dev \
#      --dry-run=client -o yaml > redis-secret.yaml
#
# 3. Convert to SealedSecret:
#    kubeseal --controller-name=sealed-secrets-controller \
#      --controller-namespace=sealed-secrets \
#      --format=yaml < redis-secret.yaml > redis-secret-sealed.yaml
#
# 4. Replace this file with the sealed secret
#
# Note: Each environment (dev/uat/production) needs its own unique password

apiVersion: v1
kind: Secret
metadata:
  name: fineract-redis-secret
  namespace: fineract-dev  # Change per environment
type: Opaque
stringData:
  redis-password: <generate-strong-password-here>
