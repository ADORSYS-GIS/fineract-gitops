# ==============================================================================
# GCP Ingress Configuration for Dev Environment
# ==============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-config
  namespace: fineract-dev
data:
  # GCP Load Balancer IP for dev environment
  apps-hostname: "34.61.82.131"
  auth-hostname: "34.61.82.131"
  cluster-issuer: selfsigned-issuer
