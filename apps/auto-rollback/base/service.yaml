apiVersion: v1
kind: Service
metadata:
  name: auto-rollback-webhook
  namespace: argocd
  labels:
    app.kubernetes.io/name: auto-rollback-webhook
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: auto-rollback-webhook
