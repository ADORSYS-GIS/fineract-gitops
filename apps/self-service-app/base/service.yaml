apiVersion: v1
kind: Service
metadata:
  name: self-service-app
  labels:
    app: self-service-app
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: self-service-app
