# ==============================================================================
# GCP Customer Self-Service Overlay for Dev Environment
# ==============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fineract-dev

resources:
  - ../../base

labels:
  - pairs:
      environment: dev
      deployment-type: gcp
      managed-by: argocd
    includeSelectors: false

patches:
  # Reduce resources and replicas for dev GCP cluster (2-node e2-medium)
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: customer-self-service
      spec:
        replicas: 1
        template:
          spec:
            serviceAccountName: fineract-gcp
            containers:
            - name: customer-self-service
              resources:
                requests:
                  cpu: "100m"
                  memory: "192Mi"
                limits:
                  cpu: "300m"
                  memory: "384Mi"
    target:
      kind: Deployment
      name: customer-self-service
