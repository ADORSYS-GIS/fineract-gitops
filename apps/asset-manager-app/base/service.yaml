apiVersion: v1
kind: Service
metadata:
  name: asset-manager-app
  labels:
    app: asset-manager-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: asset-manager-app
