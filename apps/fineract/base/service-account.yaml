---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fineract-aws
  annotations:
    # IAM Role ARN for IRSA (IAM Roles for Service Accounts)
    # This should be overridden in environment overlays with the actual role ARN
    # Example: arn:aws:iam::123456789012:role/fineract-prod-dev-fineract-irsa
    eks.amazonaws.com/role-arn: "IRSA_ROLE_ARN_PLACEHOLDER"
